<h1>Listing names</h1>

<table>
  <tr>
    <th>Fname</th>
    <th>Lname</th>
    <th>Engineer</th>
  </tr>

<% @names.each do |name| %>
  <tr>
    <td><%=h name.fname %></td>
    <td><%=h name.lname %></td>
    <td><%=h name.engineer %></td>
    <td><%= link_to 'Show', name %></td>
    <td><%= link_to 'Edit', edit_name_path(name) %></td>
    <td><%= link_to 'Destroy', name, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New name', new_name_path %>